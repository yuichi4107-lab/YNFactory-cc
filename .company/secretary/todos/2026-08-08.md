---
date: "2026-08-08"
type: daily
---

# 2026-08-08 (土)

## 最優先
- [ ] 本日14:00枠の自動生成が通り、Telegram承認→4媒体投稿まで戻ることを確認する。仕上がりで720p素材の拡大が目立たないかも目視する | 優先度: 高 | 期限: 2026-08-08

## 通常
- [ ] Drive側をGitHub最新へ `pull-sync`。**その前に**Drive側CLAUDE.mdにのみ存在する「ダウンロード事前承認」条項を `02_設定/docs/` へ移植する（移植せずpullすると14行版CLAUDE.mdに置き換わり条項が消える） | 優先度: 高
- [ ] 補充ペースの方針決め。9/14/19の3枠は1日6本の新規書き出しを消費する。現在庫は有効8本＝動画4本ぶん（約1.3日分）。書き出しが追いつかない場合は枠数を減らすか、枠ごとの形式を分けるかを決める | 優先度: 通常
- [ ] Canvasに未書き出しで残る Video 3〜7・10・11 が9:16・無字幕で使えるか確認し、使えれば次回の補充分として書き出す（新規生成前にこちらを優先） | 優先度: 通常

## 余裕があれば
- [ ]

## 完了
- [x] ハンドオフ方式を現況スナップショット型へ全面変更（HANDOFF.md 387KB→9KB、履歴を handoff-log/ 月次へ分解、tech-notes.md 新設、handoffスキル・運用ルール改訂、GitHubへpush） | 完了: 2026-08-08
- [x] Drive直下の壊れた `.git`（refs無し・desktop.ini混入）を `.git.disabled-2026-08-08` へリネームして無効化 | 完了: 2026-08-08
- [x] 定刻投稿が出なかった原因の特定と、無通知で止まる不具合の修正（Drive正本のみ。runtime未反映） | 完了: 2026-08-08
- [x] Mac runtimeへ `deploy.sh`（installなし）を実行し、安全停止通知・在庫警告・登録マージ修正を反映 | 完了: 2026-08-08
- [x] Topviewで無字幕の人物実写を6本以上書き出して登録し、安全停止を解消（Video 13〜18の6本、有効8本／未使用8本。新規生成なしでクレジット消費ゼロ） | 完了: 2026-08-08
- [x] 業務上必要なファイルのダウンロードを都度確認なしで実行してよい旨を `CLAUDE.md` へ明文化（オーナー承認済み） | 完了: 2026-08-08

## メモ・振り返り
- 8/8 09時の定刻投稿なし。直近の自動生成は8/6 19:01が最後で、8/7・8/8はqueueにも成果物にも登録がない（8/8 02:19の`2026-08-08_manual-topview-01`は手動プレビュー由来で、4媒体とも投稿済み。Xも成功している）。
- 原因は全21枠が外部リソース依存の混在形式になっていること。Topview枠は `topview_inventory.validate_inventory` が有効6本未満で `TopviewInventoryError`、`select_live_clips` が未使用2本未満で同エラー。Seedance枠はAtlas Cloud残高$0の `HTTP 402` で `HybridGenerationBlocked`。どちらも2026-08-06のオーナー決定どおりカード版へ代替せず安全停止する。
- 実在庫は事実上1〜2本（手動プレビューは `Video 12` 1本を3分割して使用）。定刻の本番経路が要求する「未使用2本」に届いていない。
- 悪化要因: `pipeline.main` の安全停止ハンドラが `notify` を呼んでおらず、**投稿がないことが完全に無通知**だった。launchdも `run_generate.sh` も終了コード1を握りつぶすため、オーナーが目視で気づくまで検知できない状態。
- 修正（Drive正本 `05_プロジェクト/shorts-factory`）: ①安全停止時にTelegramへ枠・原因・在庫残量・復旧手順を通知、②生成後に未使用が閾値以下なら補充を促す事前警告、③`register_files` を全置換から追記マージへ変更し既存の `use_count` / `last_used_at` を保持（従来は補充のたびに履歴が消え、公開済み実写が再利用され得た）、④README・config例を実消費ペース（1本=未使用2本、3枠/日=6本）に合わせて更新。
- テスト: 追加5件を含むTopview/安全停止系18件PASS。全体は 122 passed / 8 failed だが、8件はいずれもWindows環境要因（pyopenjtalk未導入のかな照合5件、cp932デコード・fcntlスタブによるロック系3件）で今回の変更とは無関係。
- 未反映: 実行環境はMacのruntime。Windowsからは `fcntl` 依存でpipelineが動かず、deployもできない。Mac側で `deploy.sh` を実行するまで本番挙動は変わらない。
- **[Mac 12:00-13:00 で解消]** `deploy.sh`（installなし）で4点修正をruntimeへ反映。`install` を付けないのは、install経路が `$DRIVE_ROOT/.company/...` を見るがDRIVE_ROOTが `05_プロジェクト` に解決されるため。launchdは登録済みで再登録不要。
- 在庫補充は**新規生成ゼロ**で解決した。Canvasに未書き出しの生成済みクリップが Video 13/14/15 だけでなく 16/17/18 まで計6本残っていたため、全部書き出して登録した（クレジット残高287.28のまま）。当初想定していた「15クレジットで新規1本」は不要だった。
- 検証: 全6本 720×1280（9:16）・15.07秒・md5相異。各プロンプトに `No on-screen text, no subtitles, no music, no generated audio` を確認。18フレーム（各3点）の目視で焼き込み文字なし、人物・濃紺スーツ・白シャツが全編一貫。結果は**有効8本／未使用8本（動画4本ぶん）**。
- `Video_12` は4媒体投稿済みのため意図的に未登録（`registered? False` を確認）。`Video_8/9` の `use_count`/`last_used_at` も追記マージで保持されており、③の修正が実機で効いていることを確認できた。
- 留意: 新規6本は720pで、既存 `Video_8/9` の1080×1920と解像度が混在する。最終レンダリングで拡大される可能性があるので初回の仕上がりは目視確認する。Topviewの「高画質化」はクレジット消費のため未実行。
- 手順メモ: Topviewの書き出しは Canvas でクリップ選択→ツールバーの ⬇ →数十秒待って `~/Downloads` を確認→`topview_assets/` へ移動→登録スクリプト。**保存の反映に時間差がある**ため、直後にファイルが無くても「ダウンロードがブロックされている」と即断しない（今回それで誤診し、不要な回り道をした）。
- オーナー承認により、業務上必要なファイルのダウンロードは都度確認なしで実行してよいことを `CLAUDE.md` の自走モードへ明文化。除外は実行可能ファイル・出所不明のファイル・認証情報を含むファイル。公開/投稿/送信と、クレジット消費を伴う新規生成は従来どおり事前承認。

